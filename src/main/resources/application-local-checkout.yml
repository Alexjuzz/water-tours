server:
  address: 127.0.0.1
payments:
  reconciliation:
    enabled: false
refunds:
  reconciliation:
    enabled: false
tickets:
  issuance:
    enabled: false
  email-delivery:
    enabled: false
management:
  health:
    mail:
      enabled: false
spring:
  jpa:
    show-sql: false
# Local development origins. The default profile lists only the two production origins.
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://water-tours.local,https://water-tours.local,http://localhost,http://localhost:3000,http://localhost:8080}
